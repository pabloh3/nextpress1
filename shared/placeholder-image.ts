/**
 * Default stand-in image for blocks and UI that need a neutral image before real media is set.
 * File is served from `client/public` (Vite exposes it at this path).
 */
export const PLACEHOLDER_IMAGE_URL = "/placeholder.png";

/** Suggested `alt` when the image is decorative or awaiting replacement content. */
export const PLACEHOLDER_IMAGE_ALT = "Placeholder image";
